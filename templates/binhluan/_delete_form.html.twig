<form method="post" action="{{ path('app_binhluan_delete', {'id': binhluan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ binhluan.id) }}">
    <button class="btn">Delete</button>
</form>
