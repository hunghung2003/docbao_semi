{% extends 'base_admin.html.twig' %}

{% block title %}Binhluan index{% endblock %}

{% block body %}
    <h1>Danh sách bình luận</h1>
<style>

</style>
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nội dung bình luận</th>
                <th>Tên người bình luận</th>
                <th>Email</th>
                <th>Title</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for binhluan in binhluans %}
            <tr>
                <td>{{ binhluan.id }}</td>
                <td>{{ binhluan.noidungbinhluan }}</td>
                <td>{{ binhluan.fullname }}</td>
                <td>{{ binhluan.email }}</td>
                <td>{{ binhluan.title }}</td>
                <td>
                    <a href="{{ path('app_binhluan_show', {'id': binhluan.id}) }}">Show</a>
                    <a href="{{ path('app_binhluan_edit', {'id': binhluan.id}) }}">Edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_binhluan_new') }}">Create new</a>
{% endblock %}
